[phases.setup]
apt_pkgs = ["ffmpeg"]

[phases.install]
commands = [
  "pip install --upgrade pip",  # Asegurar que pip está actualizado
  "pip install gunicorn",  # Instalar gunicorn manualmente
  "pip install -r requirements.txt"  # Instalar el resto de las dependencias
]

[phases.start]
commands = [
  "gunicorn -w 4 -b 0.0.0.0:$PORT main:app"
]
